apiVersion: v1
kind: Service
metadata:
  name: legal-app-service
  labels:
    app: legal-app
spec:
  selector:
    app: legal-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
